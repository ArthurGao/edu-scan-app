import { View } from 'react-native';

// Placeholder for the Scan tab — actual scan flow is handled
// by the tabPress listener in _layout.tsx which navigates to /scan
export default function ScanTabPlaceholder() {
  return <View />;
}
